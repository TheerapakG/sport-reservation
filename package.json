{
  "name": "sport-reservation",
  "type": "module",
  "private": true,
  "devDependencies": {
    "husky": "^9.1.6",
    "nx": "19.7.4",
    "vitest": "catalog:"
  },
  "scripts": {
    "prepare": "husky",
    "initialize": "nx run tiara-stack:generate && pnpm install && pnpm generate",
    "generate": "nx run-many -t generate --output-style=stream"
  },
  "pnpm": {
    "patchedDependencies": {
      "drizzle-seed@0.3.1": "patches/drizzle-seed@0.3.1.patch"
    }
  }
}
